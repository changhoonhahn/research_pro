File=Table
Product=pro

Group:
Flavor=ANY
Qualifiers=""

Common:
   Action=setup
      proddir()
      setupenv()
      pathAppend(PATH, ${UPS_PROD_DIR})
      pathAppend(LD_LIBRARY_PATH, ${UPS_PROD_DIR})
      envPrepend(IDL_PATH, +${UPS_PROD_DIR})
End:

